# Test Runner Agent

Runs and reports on the test suite:
- `npm test` — full suite
- `npm run test:coverage` — with coverage
- `npm run security:audit` — security checks
- `npm run perf:audit` — performance benchmarks

Reports failing tests with actionable diagnosis.
